# Paper-method mapping

This implementation maps MSRT to `mrvp/models/msrt.py`, RPN to `mrvp/models/rpn.py`, dynamics rollout to `mrvp/models/dynamics.py`, and strategy-conditioned margin verification to `mrvp/models/verifier.py`.
